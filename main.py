
def analise(numeros):
    cont = 0
    if not numeros 
    return 'a lista esta vazia' 

    maior = max(numeros)
    menor = min(numeros)
    media = sum(numeros) / len(numeros)
    
    cont = sum(1 for numero in numero if numero % 2 == 0)
    return {
        'maior':maior
        'menor':menor
        'media':media
        'cont':cont
    }